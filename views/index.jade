doctype html
html
	include header
	body
		div.container
			div.mars.text-center
				h1.lead.name
					|The weather on Mars from&nbsp;
					span.min !{format.temp(current.minTemp)}C&nbsp;
					|to&nbsp;
					span.max !{format.temp(current.maxTemp)}C&nbsp;
					|on !{format.shortDate(current.date)}
			div.text-center
				div.btn-group
					button#btnChart.btn.btn-default.btn-xs
						span.glyphicon.glyphicon-stats
					button#btnTable.btn.btn-default.btn-xs
						span.glyphicon.glyphicon-list
			canvas#chart(height="420", class="center-block")
			table#table.table.table-striped.hidden
				thead
					tr
						th Terrestrial Date
						th Min Temp
						th Max Temp
						th.hidden-xs Atmospheric Pressure
						th.hidden-xs Atmospheric Opacity
						th.hidden-xs Season
						th.hidden-xs Sunrise
						th.hidden-xs Sunset
				tbody
					each item in source
						tr
							td!= format.isoDate(item.terrestrial_date)
							td!= format.temp(item.min_temp)
							td!= format.temp(item.max_temp)
							td.hidden-xs= item.pressure
							td.hidden-xs= item.atmo_opacity
							td.hidden-xs= item.season
							td.hidden-xs!= format.time(item.sunrise)
							td.hidden-xs!= format.time(item.sunset)

		include footer
		include scripts
